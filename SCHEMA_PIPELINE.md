# SCHÉMA DU PIPELINE CI/CD

## Schéma simple (pour ta slide 6)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        PIPELINE CI/CD - GITHUB ACTIONS                  │
└─────────────────────────────────────────────────────────────────────────┘

    👨‍💻                    ☁️ GITHUB ACTIONS (automatique)
  Développeur
      │
      │ git push
      ▼
┌───────────┐     ┌───────────┐     ┌───────────┐     ┌───────────┐
│           │     │           │     │           │     │           │
│   PUSH    │────▶│   TESTS   │────▶│   BUILD   │────▶│  DEPLOY   │
│           │     │           │     │           │     │           │
└───────────┘     └───────────┘     └───────────┘     └───────────┘
  Trigger           npm test         npm build         Serveur
                       │                 │                 │
                       ▼                 ▼                 ▼
                   ✅ ou ❌          ✅ ou ❌          ✅ ou ❌


Si TOUT est ✅  ──────────────────────────────────────▶  🎉 Succès !

Si UN SEUL ❌  ──────────────────────────────────────▶  🛑 Arrêt + Alerte
```

---

## Schéma version simple (texte pour PowerPoint)

```
DÉVELOPPEUR  ───push───▶  GITHUB  ───déclenche───▶  PIPELINE

                              │
                              ▼
                    ┌─────────────────┐
                    │  1. CHECKOUT    │  Récupérer le code
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  2. TESTS       │  Vérifier le code
                    └────────┬────────┘
                             │
                         ✅ ou ❌
                             │
                             ▼
                    ┌─────────────────┐
                    │  3. BUILD       │  Construire l'app
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  4. DEPLOY      │  Déployer
                    └─────────────────┘
```

---

## Explication de chaque étape

| Étape | Nom | Ce qui se passe |
|-------|-----|-----------------|
| 1 | **Checkout** | GitHub récupère ton code |
| 2 | **Tests** | Exécute `npm test` pour vérifier que tout marche |
| 3 | **Build** | Compile/prépare l'application |
| 4 | **Deploy** | Envoie l'app sur le serveur (simulé ici) |

---

## Cas d'erreur

```
                    ┌─────────────────┐
                    │     TESTS       │
                    └────────┬────────┘
                             │
                         ❌ ÉCHEC
                             │
                             ▼
              ┌──────────────────────────────┐
              │  🛑 PIPELINE ARRÊTÉ          │
              │  • Pas de build              │
              │  • Pas de déploiement        │
              │  • Notification au dev       │
              └──────────────────────────────┘
```

**Le code défaillant n'arrive JAMAIS en production !**
